# Legacy Corpus Metadata

The JSON files in this directory describe an older two-document corpus build and are retained for provenance only.

The current thesis-aligned source of truth is the six-document artifact set:

- `artifacts/chunks/legal_chunks_enriched.jsonl`
- `artifacts/index/current.json`
- `artifacts/graph/legal_graph_build_summary.json`
- `artifacts/evaluation/golden_benchmark_100_extended.jsonl`

Do not use this directory as the active corpus manifest unless intentionally reproducing the earlier exploratory two-document build.
