# Final Thesis

This folder contains the final thesis rewrite as an AI/software system thesis. It uses the requested six-chapter structure and the official final evaluation artifacts under `artifacts/evaluation/final/`.

Compile from this folder with XeLaTeX or LuaLaTeX:

```powershell
latexmk -xelatex -interaction=nonstopmode -file-line-error main.tex
```

